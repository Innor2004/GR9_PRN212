<UserControl x:Class="EWMS_WPF.Views.SalesOrder.SalesOrderListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EWMS_WPF.Views.SalesOrder">
    
    <UserControl.Resources>
        <local:StatusToVisibilityConverter x:Key="StatusToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Sales Orders" FontSize="24" FontWeight="Bold"/>
            <Button Content=" Create New" HorizontalAlignment="Right"
                   Background="#4CAF50" Foreground="White"
                   Padding="20,10" FontSize="14" FontWeight="SemiBold"
                   BorderThickness="0" Cursor="Hand"
                   Command="{Binding NavigateToCreateCommand}"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
            <ComboBox SelectedValue="{Binding StatusFilter, Mode=TwoWay}" 
                     SelectedValuePath="Content"
                     Width="150" Height="35">
                <ComboBoxItem Content="All" IsSelected="True"/>
                <ComboBoxItem Content="Pending"/>
                <ComboBoxItem Content="Completed"/>
                <ComboBoxItem Content="Cancelled"/>
            </ComboBox>
            
            <TextBlock Text="Customer:" VerticalAlignment="Center" Margin="20,0,10,0" FontWeight="SemiBold"/>
            <TextBox Text="{Binding CustomerSearch, UpdateSourceTrigger=PropertyChanged}" 
                    Width="200" Height="35" VerticalContentAlignment="Center" Padding="5"/>

            <Button Content=" Search" Margin="10,0,0,0" Padding="15,8"
                   Command="{Binding LoadSalesOrdersCommand}"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
            <DataGrid ItemsSource="{Binding SalesOrders}" 
                     SelectedItem="{Binding SelectedSalesOrder}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     CanUserAddRows="False"
                     GridLinesVisibility="Horizontal"
                     RowHeight="40"
                     AlternatingRowBackground="#F9F9F9"
                     EnableRowVirtualization="False">
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="SO ID" Binding="{Binding SalesOrderId}" Width="80"/>
                    <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding CustomerPhone}" Width="120"/>
                    <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat='{}{0:N0} VND'}" Width="150"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="Pending">
                                        <Setter Property="Foreground" Value="#FF9800"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="Completed">
                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="Cancelled">
                                        <Setter Property="Foreground" Value="#F44336"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedAt, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" Width="150"/>
                    
                    <DataGridTemplateColumn Header="Actions" Width="280">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Edit" Padding="10,5" Margin="2"
                                           Background="#2196F3" Foreground="White"
                                           Click="BtnEdit_Click"
                                           Tag="{Binding SalesOrderId}"
                                           Visibility="{Binding Status, Converter={StaticResource StatusToVisibilityConverter}}"/>
                                    <Button Content="Details" Padding="10,5" Margin="2"
                                           Click="BtnDetails_Click"
                                           Tag="{Binding SalesOrderId}"/>
                                    <Button Content="Cancel" Padding="10,5" Margin="2"
                                           Background="#F44336" Foreground="White"
                                           Command="{Binding DataContext.CancelOrderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           CommandParameter="{Binding SalesOrderId}"
                                           Visibility="{Binding Status, Converter={StaticResource StatusToVisibilityConverter}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>

<UserControl x:Class="EWMS_WPF.Views.PurchaseOrder.PurchaseOrderListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EWMS_WPF.Views.PurchaseOrder">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:StatusToVisibilityConverter x:Key="StatusToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Purchase Orders" FontSize="24" FontWeight="Bold"/>
            <Button Content=" Create New" HorizontalAlignment="Right"
                   Background="#4CAF50" Foreground="White"
                   Padding="20,10" FontSize="14" FontWeight="SemiBold"
                   BorderThickness="0" Cursor="Hand"
                   Command="{Binding NavigateToCreateCommand}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                           CornerRadius="5" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- Filter -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
            <ComboBox SelectedValue="{Binding StatusFilter, Mode=TwoWay}" 
                     SelectedValuePath="Content"
                     Width="150" Height="35">
                <ComboBoxItem Content="All" IsSelected="True"/>
                <ComboBoxItem Content="Ordered"/>
                <ComboBoxItem Content="ReadyToReceive"/>
                <ComboBoxItem Content="Received"/>
                <ComboBoxItem Content="Cancelled"/>
            </ComboBox>
            
            <Button Content=" Refresh" Margin="15,0,0,0" Padding="15,8"
                   Command="{Binding LoadPurchaseOrdersCommand}"/>
        </StackPanel>

        <!-- DataGrid -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
            <DataGrid ItemsSource="{Binding PurchaseOrders}" 
                     SelectedItem="{Binding SelectedPurchaseOrder}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     CanUserAddRows="False"
                     GridLinesVisibility="Horizontal"
                     RowHeight="40"
                     AlternatingRowBackground="#F9F9F9">
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PO ID" Binding="{Binding PurchaseOrderId}" Width="80"/>
                    <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier.SupplierName}" Width="*"/>
                    <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat='{}{0:N0} VND'}" Width="150"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="Ordered">
                                        <Setter Property="Foreground" Value="#FF9800"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="ReadyToReceive">
                                        <Setter Property="Foreground" Value="#2196F3"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="Received">
                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="Cancelled">
                                        <Setter Property="Foreground" Value="#F44336"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedAt, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" Width="150"/>
                    <DataGridTextColumn Header="Expected Date" Binding="{Binding ExpectedReceivingDate, StringFormat='{}{0:dd/MM/yyyy}'}" Width="120"/>
                    
                    <DataGridTemplateColumn Header="Actions" Width="300">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Details" Padding="10,5" Margin="2"
                                           Click="BtnDetails_Click"
                                           Tag="{Binding PurchaseOrderId}"/>
                                    <Button Content="Mark Delivered" Padding="10,5" Margin="2"
                                           Background="#2196F3" Foreground="White"
                                           Command="{Binding DataContext.MarkAsDeliveredCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           CommandParameter="{Binding PurchaseOrderId}"
                                           Visibility="{Binding Status, Converter={StaticResource StatusToVisibilityConverter}}"/>
                                    <Button Content="Cancel" Padding="10,5" Margin="2"
                                           Background="#FF9800" Foreground="White"
                                           Command="{Binding DataContext.CancelOrderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           CommandParameter="{Binding PurchaseOrderId}"
                                           Visibility="{Binding Status, Converter={StaticResource StatusToVisibilityConverter}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3" Background="#80000000" 
               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Loading..." FontSize="20" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

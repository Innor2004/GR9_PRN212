<UserControl x:Class="EWMS_WPF.Views.StockIn.StockInHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Stock In History" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="View completed and cancelled purchase orders" FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Search by PO ID:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="150" Height="35" VerticalContentAlignment="Center" Padding="5"/>
            <Button Content=" Search" Margin="10,0,0,0" Padding="15,8" Command="{Binding SearchCommand}"/>
            <Button Content=" Back to List" Margin="20,0,0,0" Padding="15,8" Command="{Binding BackToListCommand}" Background="#9E9E9E" Foreground="White" BorderThickness="0"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
            <DataGrid ItemsSource="{Binding HistoryOrders}" 
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     CanUserAddRows="False"
                     RowHeight="40"
                     AlternatingRowBackground="#F9F9F9">
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PO ID" Binding="{Binding PurchaseOrderId}" Width="1*"/>
                    <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier.SupplierName}" Width="2*"/>
                    <DataGridTemplateColumn Header="Products" Width="5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ProductsText}" TextWrapping="Wrap" Margin="5,2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Order Date" Binding="{Binding CreatedAt, StringFormat='{}{0:dd/MM/yyyy}'}" Width="2*"/>
                    <DataGridTextColumn Header="Expected Date" Binding="{Binding ExpectedReceivingDate, StringFormat='{}{0:dd/MM/yyyy}'}" Width="2*"/>
                    <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat='{}{0:N0} VND'}" Width="2*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="2*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="Received">
                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <Trigger Property="Text" Value="Cancelled">
                                        <Setter Property="Foreground" Value="#F44336"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>

<UserControl x:Class="EWMS_WPF.Views.StockIn.StockInListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Stock In - Receive Goods" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Select purchase orders ready to receive" FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Search by PO ID:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="150" Height="35" VerticalContentAlignment="Center" Padding="5"/>
            <Button Content="🔍 Search" Margin="10,0,0,0" Padding="15,8" Command="{Binding SearchCommand}"/>
            <Button Content="🔄 Refresh" Margin="10,0,0,0" Padding="15,8" Command="{Binding LoadPendingOrdersCommand}"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
            <DataGrid ItemsSource="{Binding PendingPurchaseOrders}" 
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     CanUserAddRows="False"
                     RowHeight="40"
                     AlternatingRowBackground="#F9F9F9">
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PO ID" Binding="{Binding PurchaseOrderId}" Width="80"/>
                    <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier.SupplierName}" Width="*"/>
                    <DataGridTextColumn Header="Order Date" Binding="{Binding CreatedAt, StringFormat='{}{0:dd/MM/yyyy}'}" Width="120"/>
                    <DataGridTextColumn Header="Expected Date" Binding="{Binding ExpectedReceivingDate, StringFormat='{}{0:dd/MM/yyyy}'}" Width="120"/>
                    <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat='{}{0:N0} VND'}" Width="150"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="150"/>
                    
                    <DataGridTemplateColumn Header="Actions" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Receive Goods" Padding="10,5"
                                       Background="#4CAF50" Foreground="White"
                                       Tag="{Binding PurchaseOrderId}"
                                       Click="BtnReceive_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
